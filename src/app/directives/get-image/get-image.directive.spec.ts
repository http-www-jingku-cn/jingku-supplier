import { GetImageDirective } from './get-image.directive';

describe('GetImageDirective', () => {
  it('should create an instance', () => {
    const directive = new GetImageDirective();
    expect(directive).toBeTruthy();
  });
});
