import { PreviewImagesDirective } from './preview-images.directive';

describe('PreviewImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewImagesDirective();
    expect(directive).toBeTruthy();
  });
});
