import { BypassSecurityTrustResourceurlPipe } from './bypass-security-trust-resourceurl.pipe';

describe('BypassSecurityTrustResourceurlPipe', () => {
  it('create an instance', () => {
    const pipe = new BypassSecurityTrustResourceurlPipe();
    expect(pipe).toBeTruthy();
  });
});
