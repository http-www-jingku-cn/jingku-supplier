import { BypassSecurityTrustHtmlPipe } from './bypass-security-trust-html.pipe';

describe('BypassSecurityTrustHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new BypassSecurityTrustHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
