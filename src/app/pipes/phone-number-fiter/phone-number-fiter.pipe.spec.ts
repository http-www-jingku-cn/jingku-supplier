import { PhoneNumberFiterPipe } from './phone-number-fiter.pipe';

describe('PhoneNumberFiterPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneNumberFiterPipe();
    expect(pipe).toBeTruthy();
  });
});
